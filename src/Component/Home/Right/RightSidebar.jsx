import { getAuth } from 'firebase/auth';
import React from 'react';


const RightSidebar = () => {
    let auth = getAuth();
    
    return (
        <div>
        </div>
    );
};

export default RightSidebar;